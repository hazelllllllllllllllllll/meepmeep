export class WorkerLogger {
    errors = [];
    error(msg) {
        this.errors.push(msg);
    }
    flush() {
        const errors = this.errors.join("\n");
        this.errors = [];
        return {
            errors,
        };
    }
}
