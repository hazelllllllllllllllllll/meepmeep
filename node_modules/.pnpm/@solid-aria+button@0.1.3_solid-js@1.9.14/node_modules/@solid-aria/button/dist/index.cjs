"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  createButton: () => createButton,
  createToggleButton: () => createToggleButton
});
module.exports = __toCommonJS(src_exports);

// src/createButton.ts
var import_focus = require("@solid-aria/focus");
var import_interactions = require("@solid-aria/interactions");
var import_utils = require("@solid-aria/utils");
var import_props = require("@solid-primitives/props");
var import_solid_js = require("solid-js");
function createButton(props, ref) {
  const defaultProps = {
    elementType: "button",
    type: "button"
  };
  props = (0, import_solid_js.mergeProps)(defaultProps, props);
  const additionalButtonElementProps = {
    get type() {
      return props.type;
    },
    get disabled() {
      return props.isDisabled;
    }
  };
  const additionalOtherElementProps = {
    role: "button",
    get tabIndex() {
      return props.isDisabled ? void 0 : 0;
    },
    get href() {
      return props.elementType === "a" && props.isDisabled ? void 0 : props.href;
    },
    get target() {
      return props.elementType === "a" ? props.target : void 0;
    },
    get type() {
      return props.elementType === "input" ? props.type : void 0;
    },
    get disabled() {
      return props.elementType === "input" ? props.isDisabled : void 0;
    },
    get "aria-disabled"() {
      return !props.isDisabled || props.elementType === "input" ? void 0 : props.isDisabled;
    },
    get rel() {
      return props.elementType === "a" ? props.rel : void 0;
    }
  };
  const additionalProps = (0, import_solid_js.mergeProps)((0, import_solid_js.createMemo)(() => {
    return props.elementType === "button" ? additionalButtonElementProps : additionalOtherElementProps;
  }));
  const [createPressProps] = (0, import_solid_js.splitProps)(props, [
    "onPressStart",
    "onPressEnd",
    "onPressChange",
    "onPress",
    "isDisabled",
    "preventFocusOnPress"
  ]);
  const { pressProps, isPressed } = (0, import_interactions.createPress)(createPressProps, ref);
  const { focusableProps } = (0, import_focus.createFocusable)(props, ref);
  const domProps = (0, import_solid_js.mergeProps)((0, import_solid_js.createMemo)(() => (0, import_utils.filterDOMProps)(props, { labelable: true })));
  const baseButtonProps = {
    get "aria-haspopup"() {
      return props["aria-haspopup"];
    },
    get "aria-expanded"() {
      return props["aria-expanded"];
    },
    get "aria-controls"() {
      return props["aria-controls"];
    },
    get "aria-pressed"() {
      return props["aria-pressed"];
    },
    get tabIndex() {
      return props.allowFocusWhenDisabled && props.isDisabled ? -1 : focusableProps.tabIndex;
    },
    onClick: (e) => {
      if (!props.onClick) {
        return;
      }
      props.onClick(e);
      console.warn("onClick is deprecated, please use onPress");
    }
  };
  const buttonProps = (0, import_props.combineProps)(additionalProps, focusableProps, pressProps, domProps, baseButtonProps);
  return { buttonProps, isPressed };
}

// src/createToggleButton.ts
var import_toggle = require("@solid-aria/toggle");
var import_props2 = require("@solid-primitives/props");
var import_utils2 = require("@solid-primitives/utils");
var import_solid_js2 = require("solid-js");
function createToggleButton(props, ref) {
  const state = (0, import_toggle.createToggleState)(props);
  const createButtonProps = (0, import_solid_js2.mergeProps)(props, {
    onPress: (0, import_utils2.chain)([state.toggle, props.onPress])
  });
  const { isPressed, buttonProps } = createButton(createButtonProps, ref);
  const toggleButtonProps = {
    get "aria-pressed"() {
      return state.isSelected();
    }
  };
  return {
    buttonProps: (0, import_props2.combineProps)(buttonProps, toggleButtonProps),
    isPressed,
    state
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createButton,
  createToggleButton
});
