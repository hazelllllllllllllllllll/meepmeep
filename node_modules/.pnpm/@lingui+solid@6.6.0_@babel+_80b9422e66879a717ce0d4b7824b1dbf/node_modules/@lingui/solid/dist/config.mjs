import { defineConfig as defineConfig$1 } from '@lingui/conf';

const solidRuntimeModules = {
  Trans: ["@lingui/solid", "Trans"],
  useLingui: ["@lingui/solid", "useLingui"]
};
function defineConfig(config) {
  const {
    runtimeConfigModule,
    macro,
    ...rest
  } = config;
  const runtimeConfig = Array.isArray(runtimeConfigModule) ? {
    i18n: runtimeConfigModule
  } : runtimeConfigModule;
  const jsxPackage = macro?.jsxPackage ? Array.from([...macro.jsxPackage, "@lingui/solid/macro"]) : ["@lingui/solid/macro"];
  return defineConfig$1({
    ...rest,
    macro: {
      ...macro,
      jsxPackage,
      jsxRuntime: macro?.jsxRuntime ?? "solid"
    },
    runtimeConfigModule: {
      ...solidRuntimeModules,
      ...runtimeConfig
    }
  });
}

export { defineConfig };
