"use strict";
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  PressResponderContext: () => PressResponderContext,
  createFocus: () => createFocus,
  createFocusVisible: () => createFocusVisible,
  createFocusVisibleListener: () => createFocusVisibleListener,
  createFocusWithin: () => createFocusWithin,
  createHover: () => createHover,
  createInteractOutside: () => createInteractOutside,
  createInteractionModality: () => createInteractionModality,
  createKeyboard: () => createKeyboard,
  createLongPress: () => createLongPress,
  createPress: () => createPress,
  disableTextSelection: () => disableTextSelection,
  getInteractionModality: () => getInteractionModality,
  isKeyboardFocusVisible: () => isKeyboardFocusVisible,
  restoreTextSelection: () => restoreTextSelection,
  setInteractionModality: () => setInteractionModality,
  setupGlobalFocusEvents: () => setupGlobalFocusEvents
});
module.exports = __toCommonJS(src_exports);

// src/createFocus.ts
var import_utils = require("@solid-primitives/utils");

// src/utils.ts
var import_solid_js = require("solid-js");
function isVirtualClick(event) {
  if (event.mozInputSource === 0 && event.isTrusted) {
    return true;
  }
  return event.detail === 0 && !event.pointerType;
}
function createSyntheticBlurEvent(onBlur) {
  const [isFocused, setIsFocused] = (0, import_solid_js.createSignal)(false);
  const [observer, setObserver] = (0, import_solid_js.createSignal)(null);
  const onFocus = (e) => {
    var _a;
    if (e.target instanceof HTMLButtonElement || e.target instanceof HTMLInputElement || e.target instanceof HTMLTextAreaElement || e.target instanceof HTMLSelectElement) {
      setIsFocused(true);
      const target = e.target;
      const onBlurHandler = (e2) => {
        var _a2;
        setIsFocused(false);
        if (target.disabled) {
          onBlur(new FocusEvent("blur", e2));
        }
        (_a2 = observer()) == null ? void 0 : _a2.disconnect();
        setObserver(null);
      };
      target.addEventListener("focusout", onBlurHandler, { once: true });
      const mutationCallback = () => {
        var _a2;
        if (isFocused() && target.disabled) {
          (_a2 = observer()) == null ? void 0 : _a2.disconnect();
          target.dispatchEvent(new FocusEvent("blur"));
          target.dispatchEvent(new FocusEvent("focusout", { bubbles: true }));
        }
      };
      setObserver(new MutationObserver(mutationCallback));
      (_a = observer()) == null ? void 0 : _a.observe(target, { attributes: true, attributeFilter: ["disabled"] });
    }
  };
  (0, import_solid_js.onCleanup)(() => {
    var _a;
    (_a = observer()) == null ? void 0 : _a.disconnect();
    setObserver(null);
  });
  return onFocus;
}

// src/createFocus.ts
function createFocus(props) {
  const isDisabled = () => (0, import_utils.access)(props.isDisabled) ?? false;
  const onBlur = (e) => {
    var _a, _b;
    if (isDisabled() || !props.onBlur && !props.onFocusChange) {
      return;
    }
    (_a = props.onBlur) == null ? void 0 : _a.call(props, e);
    (_b = props.onFocusChange) == null ? void 0 : _b.call(props, false);
  };
  const onSyntheticFocus = createSyntheticBlurEvent(onBlur);
  const onFocus = (e) => {
    var _a, _b;
    if (isDisabled() || !props.onFocus && !props.onBlur && !props.onFocusChange) {
      return;
    }
    (_a = props.onFocus) == null ? void 0 : _a.call(props, e);
    (_b = props.onFocusChange) == null ? void 0 : _b.call(props, true);
    onSyntheticFocus(e);
  };
  const focusProps = {
    onFocus,
    onBlur
  };
  return { focusProps };
}

// src/createFocusVisible.ts
var import_platform = require("@solid-primitives/platform");
var import_utils3 = require("@solid-primitives/utils");
var import_solid_js2 = require("solid-js");
var changeHandlers = /* @__PURE__ */ new Set();
var currentModality;
var hasSetupGlobalListeners = false;
var hasEventBeforeFocus = false;
var hasBlurredWindowRecently = false;
var FOCUS_VISIBLE_INPUT_KEYS = {
  Tab: true,
  Escape: true
};
function triggerChangeHandlers(modality, e) {
  for (const handler of changeHandlers) {
    handler(modality, e);
  }
}
function isValidKey(e) {
  return !(e.metaKey || !import_platform.isMac && e.altKey || e.ctrlKey || e.key === "Control" || e.key === "Shift" || e.key === "Meta");
}
function handleKeyboardEvent(e) {
  hasEventBeforeFocus = true;
  if (isValidKey(e)) {
    currentModality = "keyboard";
    triggerChangeHandlers("keyboard", e);
  }
}
function handlePointerEvent(e) {
  currentModality = "pointer";
  if (e.type === "mousedown" || e.type === "pointerdown") {
    hasEventBeforeFocus = true;
    triggerChangeHandlers("pointer", e);
  }
}
function handleClickEvent(e) {
  if (isVirtualClick(e)) {
    hasEventBeforeFocus = true;
    currentModality = "virtual";
  }
}
function handleFocusEvent(e) {
  if (e.target === window || e.target === document) {
    return;
  }
  if (!hasEventBeforeFocus && !hasBlurredWindowRecently) {
    currentModality = "virtual";
    triggerChangeHandlers("virtual", e);
  }
  hasEventBeforeFocus = false;
  hasBlurredWindowRecently = false;
}
function handleWindowBlur() {
  hasEventBeforeFocus = false;
  hasBlurredWindowRecently = true;
}
function setupGlobalFocusEvents() {
  if (typeof window === "undefined" || hasSetupGlobalListeners) {
    return;
  }
  const focus = HTMLElement.prototype.focus;
  HTMLElement.prototype.focus = function(...args) {
    hasEventBeforeFocus = true;
    focus.apply(this, args);
  };
  document.addEventListener("keydown", handleKeyboardEvent, true);
  document.addEventListener("keyup", handleKeyboardEvent, true);
  document.addEventListener("click", handleClickEvent, true);
  window.addEventListener("focus", handleFocusEvent, true);
  window.addEventListener("blur", handleWindowBlur, false);
  if (typeof PointerEvent !== "undefined") {
    document.addEventListener("pointerdown", handlePointerEvent, true);
    document.addEventListener("pointermove", handlePointerEvent, true);
    document.addEventListener("pointerup", handlePointerEvent, true);
  } else {
    document.addEventListener("mousedown", handlePointerEvent, true);
    document.addEventListener("mousemove", handlePointerEvent, true);
    document.addEventListener("mouseup", handlePointerEvent, true);
  }
  hasSetupGlobalListeners = true;
}
if (typeof document !== "undefined") {
  if (document.readyState !== "loading") {
    setupGlobalFocusEvents();
  } else {
    document.addEventListener("DOMContentLoaded", setupGlobalFocusEvents);
  }
}
function isKeyboardFocusVisible() {
  return currentModality !== "pointer";
}
function getInteractionModality() {
  return currentModality;
}
function setInteractionModality(modality) {
  currentModality = modality;
  triggerChangeHandlers(modality);
}
function createInteractionModality() {
  setupGlobalFocusEvents();
  const [modality, setModality] = (0, import_solid_js2.createSignal)(currentModality);
  (0, import_solid_js2.onMount)(() => {
    const handler = () => {
      setModality(currentModality);
    };
    changeHandlers.add(handler);
    (0, import_solid_js2.onCleanup)(() => {
      changeHandlers.delete(handler);
    });
  });
  return modality;
}
function isKeyboardFocusEvent(isTextInput, modality, e) {
  return !(isTextInput && modality === "keyboard" && e instanceof KeyboardEvent && !FOCUS_VISIBLE_INPUT_KEYS[e.key]);
}
function createFocusVisibleListener(fn, dep, opts) {
  setupGlobalFocusEvents();
  (0, import_solid_js2.createEffect)((0, import_solid_js2.on)(() => dep(), () => {
    const handler = (modality, e) => {
      if (!isKeyboardFocusEvent(!!(opts == null ? void 0 : opts.isTextInput), modality, e)) {
        return;
      }
      fn(isKeyboardFocusVisible());
    };
    changeHandlers.add(handler);
    (0, import_solid_js2.onCleanup)(() => {
      changeHandlers.delete(handler);
    });
  }));
}
function createFocusVisible(props = {}) {
  const [isFocusVisible, setFocusVisible] = (0, import_solid_js2.createSignal)((0, import_utils3.access)(props.autofocus) || isKeyboardFocusVisible());
  createFocusVisibleListener(setFocusVisible, () => (0, import_utils3.access)(props.isTextInput), {
    isTextInput: (0, import_utils3.access)(props.isTextInput)
  });
  return { isFocusVisible };
}

// src/createFocusWithin.ts
var import_utils5 = require("@solid-primitives/utils");
var import_solid_js3 = require("solid-js");
function createFocusWithin(props) {
  const [isFocusWithin, setIsFocusWithin] = (0, import_solid_js3.createSignal)(false);
  const isDisabled = () => (0, import_utils5.access)(props.isDisabled) ?? false;
  const onFocusOut = (e) => {
    var _a, _b;
    if (isDisabled()) {
      return;
    }
    const currentTarget = e.currentTarget;
    const relatedTarget = e.relatedTarget;
    if (isFocusWithin() && !(currentTarget == null ? void 0 : currentTarget.contains(relatedTarget))) {
      setIsFocusWithin(false);
      (_a = props.onFocusOut) == null ? void 0 : _a.call(props, e);
      (_b = props.onFocusWithinChange) == null ? void 0 : _b.call(props, false);
    }
  };
  const onSyntheticFocus = createSyntheticBlurEvent(onFocusOut);
  const onFocusIn = (e) => {
    var _a, _b;
    if (isDisabled()) {
      return;
    }
    if (!isFocusWithin()) {
      (_a = props.onFocusIn) == null ? void 0 : _a.call(props, e);
      (_b = props.onFocusWithinChange) == null ? void 0 : _b.call(props, true);
      setIsFocusWithin(true);
      onSyntheticFocus(e);
    }
  };
  const focusWithinProps = {
    onFocusIn,
    onFocusOut
  };
  return { focusWithinProps };
}

// src/createHover.ts
var import_utils7 = require("@solid-primitives/utils");
var import_solid_js4 = require("solid-js");
var globalIgnoreEmulatedMouseEvents = false;
var hoverCount = 0;
function setGlobalIgnoreEmulatedMouseEvents() {
  globalIgnoreEmulatedMouseEvents = true;
  setTimeout(() => {
    globalIgnoreEmulatedMouseEvents = false;
  }, 50);
}
function handleGlobalPointerEvent(e) {
  if (e.pointerType === "touch") {
    setGlobalIgnoreEmulatedMouseEvents();
  }
}
function setupGlobalTouchEvents() {
  if (typeof document === "undefined") {
    return;
  }
  if (typeof PointerEvent !== "undefined") {
    document.addEventListener("pointerup", handleGlobalPointerEvent);
  } else {
    document.addEventListener("touchend", setGlobalIgnoreEmulatedMouseEvents);
  }
  hoverCount++;
  return () => {
    hoverCount--;
    if (hoverCount > 0) {
      return;
    }
    if (typeof PointerEvent !== "undefined") {
      document.removeEventListener("pointerup", handleGlobalPointerEvent);
    } else {
      document.removeEventListener("touchend", setGlobalIgnoreEmulatedMouseEvents);
    }
  };
}
function createHover(props = {}) {
  const [isHovered, setIsHovered] = (0, import_solid_js4.createSignal)(false);
  const [ignoreEmulatedMouseEvents, setIgnoreEmulatedMouseEvents] = (0, import_solid_js4.createSignal)(false);
  const [pointerType, setPointerType] = (0, import_solid_js4.createSignal)("");
  const [target, setTarget] = (0, import_solid_js4.createSignal)(null);
  const triggerHoverStart = (event, pointerType2) => {
    var _a, _b;
    setPointerType(pointerType2);
    const eventCurrentTarget = event.currentTarget;
    const eventTarget = event.target;
    if ((0, import_utils7.access)(props.isDisabled) || pointerType2 === "touch" || isHovered() || !(eventCurrentTarget == null ? void 0 : eventCurrentTarget.contains(eventTarget))) {
      return;
    }
    setTarget(eventCurrentTarget);
    (_a = props.onHoverStart) == null ? void 0 : _a.call(props, {
      type: "hoverstart",
      target: eventCurrentTarget,
      pointerType: pointerType2
    });
    (_b = props.onHoverChange) == null ? void 0 : _b.call(props, true);
    setIsHovered(true);
  };
  const triggerHoverEnd = (event, pointerType2) => {
    var _a, _b;
    setPointerType("");
    setTarget(null);
    if (pointerType2 === "touch" || !isHovered() || !event.currentTarget) {
      return;
    }
    (_a = props.onHoverEnd) == null ? void 0 : _a.call(props, {
      type: "hoverend",
      target: event.currentTarget,
      pointerType: pointerType2
    });
    (_b = props.onHoverChange) == null ? void 0 : _b.call(props, false);
    setIsHovered(false);
  };
  const hoverProps = {};
  if (typeof PointerEvent !== "undefined") {
    hoverProps.onPointerEnter = (e) => {
      if (globalIgnoreEmulatedMouseEvents && e.pointerType === "mouse") {
        return;
      }
      triggerHoverStart(e, e.pointerType);
    };
    hoverProps.onPointerLeave = (e) => {
      const eventCurrentTarget = e.currentTarget;
      const eventTarget = e.target;
      if ((0, import_utils7.access)(props.isDisabled) || !(eventCurrentTarget == null ? void 0 : eventCurrentTarget.contains(eventTarget))) {
        return;
      }
      triggerHoverEnd(e, e.pointerType);
    };
  } else {
    hoverProps.onTouchStart = () => {
      setIgnoreEmulatedMouseEvents(true);
    };
    hoverProps.onMouseEnter = (e) => {
      if (!ignoreEmulatedMouseEvents() && !globalIgnoreEmulatedMouseEvents) {
        triggerHoverStart(e, "mouse");
      }
      setIgnoreEmulatedMouseEvents(false);
    };
    hoverProps.onMouseLeave = (e) => {
      const eventCurrentTarget = e.currentTarget;
      const eventTarget = e.target;
      if ((0, import_utils7.access)(props.isDisabled) || !(eventCurrentTarget == null ? void 0 : eventCurrentTarget.contains(eventTarget))) {
        return;
      }
      triggerHoverEnd(e, "mouse");
    };
  }
  (0, import_solid_js4.onMount)(() => {
    const cleanupFn = setupGlobalTouchEvents();
    (0, import_solid_js4.onCleanup)(() => cleanupFn == null ? void 0 : cleanupFn());
  });
  (0, import_solid_js4.createEffect)((0, import_solid_js4.on)(() => (0, import_utils7.access)(props.isDisabled), (isDisabled) => {
    if (isDisabled) {
      triggerHoverEnd({ currentTarget: target() }, pointerType());
    }
  }));
  return { isHovered, hoverProps };
}

// src/createInteractOutside.ts
var import_utils8 = require("@solid-primitives/utils");
var import_solid_js5 = require("solid-js");
function createInteractOutside(props, ref) {
  const [isPointerDown, setIsPointerDown] = (0, import_solid_js5.createSignal)(false);
  const [ignoreEmulatedMouseEvents, setIgnoreEmulatedMouseEvents] = (0, import_solid_js5.createSignal)(false);
  (0, import_solid_js5.createEffect)(() => {
    if ((0, import_utils8.access)(props.isDisabled)) {
      return;
    }
    const onPointerDown = (e) => {
      var _a;
      if (isValidEvent(e, ref())) {
        (_a = props.onInteractOutsideStart) == null ? void 0 : _a.call(props, e);
        setIsPointerDown(true);
      }
    };
    if (typeof PointerEvent !== "undefined") {
      const onPointerUp = (e) => {
        var _a;
        if (isPointerDown() && isValidEvent(e, ref())) {
          setIsPointerDown(false);
          (_a = props.onInteractOutside) == null ? void 0 : _a.call(props, e);
        }
      };
      document.addEventListener("pointerdown", onPointerDown, true);
      document.addEventListener("pointerup", onPointerUp, true);
      (0, import_solid_js5.onCleanup)(() => {
        document.removeEventListener("pointerdown", onPointerDown, true);
        document.removeEventListener("pointerup", onPointerUp, true);
      });
    } else {
      const onMouseUp = (e) => {
        var _a;
        if (ignoreEmulatedMouseEvents()) {
          setIgnoreEmulatedMouseEvents(false);
        } else if (isPointerDown() && isValidEvent(e, ref())) {
          setIsPointerDown(false);
          (_a = props.onInteractOutside) == null ? void 0 : _a.call(props, e);
        }
      };
      const onTouchEnd = (e) => {
        var _a;
        setIgnoreEmulatedMouseEvents(true);
        if (isPointerDown() && isValidEvent(e, ref())) {
          setIsPointerDown(false);
          (_a = props.onInteractOutside) == null ? void 0 : _a.call(props, e);
        }
      };
      document.addEventListener("mousedown", onPointerDown, true);
      document.addEventListener("mouseup", onMouseUp, true);
      document.addEventListener("touchstart", onPointerDown, true);
      document.addEventListener("touchend", onTouchEnd, true);
      (0, import_solid_js5.onCleanup)(() => {
        document.removeEventListener("mousedown", onPointerDown, true);
        document.removeEventListener("mouseup", onMouseUp, true);
        document.removeEventListener("touchstart", onPointerDown, true);
        document.removeEventListener("touchend", onTouchEnd, true);
      });
    }
  });
}
function isValidEvent(event, ref) {
  if (event.button > 0) {
    return false;
  }
  if (event.target) {
    const ownerDocument = event.target.ownerDocument;
    if (!ownerDocument || !ownerDocument.documentElement.contains(event.target)) {
      return false;
    }
  }
  return ref && !ref.contains(event.target);
}

// src/createKeyboard.ts
var import_utils9 = require("@solid-primitives/utils");
function createKeyboard(props) {
  const isDisabled = () => (0, import_utils9.access)(props.isDisabled) ?? false;
  const onKeyDown = (event) => {
    var _a;
    if (isDisabled()) {
      return;
    }
    (_a = props.onKeyDown) == null ? void 0 : _a.call(props, event);
  };
  const onKeyUp = (event) => {
    var _a;
    if (isDisabled()) {
      return;
    }
    (_a = props.onKeyUp) == null ? void 0 : _a.call(props, event);
  };
  const keyboardProps = {
    onKeyDown,
    onKeyUp
  };
  return { keyboardProps };
}

// src/createLongPress.ts
var import_utils14 = require("@solid-aria/utils");
var import_props2 = require("@solid-primitives/props");
var import_utils15 = require("@solid-primitives/utils");
var import_solid_js7 = require("solid-js");

// src/createPress.ts
var import_utils11 = require("@solid-aria/utils");
var import_props = require("@solid-primitives/props");
var import_utils12 = require("@solid-primitives/utils");
var import_solid_js6 = require("solid-js");

// src/textSelection.ts
var import_utils10 = require("@solid-aria/utils");
var import_platform2 = require("@solid-primitives/platform");
var state = "default";
var savedUserSelect = "";
var modifiedElementMap = /* @__PURE__ */ new WeakMap();
function disableTextSelection(target) {
  if (import_platform2.isIOS) {
    if (state === "default") {
      savedUserSelect = document.documentElement.style.webkitUserSelect;
      document.documentElement.style.webkitUserSelect = "none";
    }
    state = "disabled";
  } else if (target) {
    modifiedElementMap.set(target, target.style.userSelect);
    target.style.userSelect = "none";
  }
}
function restoreTextSelection(target) {
  if (import_platform2.isIOS) {
    if (state !== "disabled") {
      return;
    }
    state = "restoring";
    setTimeout(() => {
      (0, import_utils10.runAfterTransition)(() => {
        if (state === "restoring") {
          if (document.documentElement.style.webkitUserSelect === "none") {
            document.documentElement.style.webkitUserSelect = savedUserSelect || "";
          }
          savedUserSelect = "";
          state = "default";
        }
      });
    }, 300);
  } else {
    if (target && modifiedElementMap.has(target)) {
      const targetOldUserSelect = modifiedElementMap.get(target) ?? "";
      if (target.style.userSelect === "none") {
        target.style.userSelect = targetOldUserSelect;
      }
      if (target.getAttribute("style") === "") {
        target.removeAttribute("style");
      }
      modifiedElementMap.delete(target);
    }
  }
}

// src/createPress.ts
var PressResponderContext = (0, import_solid_js6.createContext)();
function usePressResponderContext(props, ref) {
  const context = (0, import_solid_js6.useContext)(PressResponderContext);
  if (!context) {
    return props;
  }
  const [, contextProps] = (0, import_solid_js6.splitProps)(context, ["register", "ref"]);
  props = (0, import_props.combineProps)(contextProps, props);
  context.register();
  if (ref) {
    (0, import_utils11.createSyncRef)(context, ref);
  }
  return props;
}
function createPress(props, ref) {
  const [local, domProps] = (0, import_solid_js6.splitProps)(usePressResponderContext(props, ref), [
    "onPress",
    "onPressChange",
    "onPressStart",
    "onPressEnd",
    "onPressUp",
    "isDisabled",
    "isPressed",
    "preventFocusOnPress",
    "shouldCancelOnPointerExit",
    "allowTextSelectionOnPress"
  ]);
  const [isTriggerPressed, setIsTriggerPressed] = (0, import_solid_js6.createSignal)(false);
  const [_isPressed, _setIsPressed] = (0, import_solid_js6.createSignal)(false);
  const [ignoreEmulatedMouseEvents, setIgnoreEmulatedMouseEvents] = (0, import_solid_js6.createSignal)(false);
  const [ignoreClickAfterPress, setIgnoreClickAfterPress] = (0, import_solid_js6.createSignal)(false);
  const [didFirePressStart, setDidFirePressStart] = (0, import_solid_js6.createSignal)(false);
  const [activePointerId, setActivePointerId] = (0, import_solid_js6.createSignal)(null);
  const [target, setTarget] = (0, import_solid_js6.createSignal)(null);
  const [isOverTarget, setIsOverTarget] = (0, import_solid_js6.createSignal)(false);
  const [pointerType, setPointerType] = (0, import_solid_js6.createSignal)(null);
  const { addGlobalListener, removeAllGlobalListeners } = (0, import_utils11.createGlobalListeners)();
  const isPressed = () => (0, import_utils12.access)(local.isPressed) ?? isTriggerPressed();
  const triggerPressStart = (originalEvent, pointerType2) => {
    var _a, _b;
    if ((0, import_utils12.access)(local.isDisabled) || didFirePressStart()) {
      return;
    }
    (_a = local.onPressStart) == null ? void 0 : _a.call(local, {
      type: "pressstart",
      pointerType: pointerType2,
      target: originalEvent.currentTarget,
      shiftKey: originalEvent.shiftKey,
      metaKey: originalEvent.metaKey,
      ctrlKey: originalEvent.ctrlKey,
      altKey: originalEvent.altKey
    });
    (_b = local.onPressChange) == null ? void 0 : _b.call(local, true);
    setDidFirePressStart(true);
    setIsTriggerPressed(true);
  };
  const triggerPressEnd = (originalEvent, pointerType2, wasPressed = true) => {
    var _a, _b, _c;
    if (!didFirePressStart()) {
      return;
    }
    setIgnoreClickAfterPress(true);
    setDidFirePressStart(false);
    (_a = local.onPressEnd) == null ? void 0 : _a.call(local, {
      type: "pressend",
      pointerType: pointerType2,
      target: originalEvent.currentTarget,
      shiftKey: originalEvent.shiftKey,
      metaKey: originalEvent.metaKey,
      ctrlKey: originalEvent.ctrlKey,
      altKey: originalEvent.altKey
    });
    (_b = local.onPressChange) == null ? void 0 : _b.call(local, false);
    setIsTriggerPressed(false);
    if (wasPressed && !(0, import_utils12.access)(local.isDisabled)) {
      (_c = local.onPress) == null ? void 0 : _c.call(local, {
        type: "press",
        pointerType: pointerType2,
        target: originalEvent.currentTarget,
        shiftKey: originalEvent.shiftKey,
        metaKey: originalEvent.metaKey,
        ctrlKey: originalEvent.ctrlKey,
        altKey: originalEvent.altKey
      });
    }
  };
  const triggerPressUp = (originalEvent, pointerType2) => {
    var _a;
    if ((0, import_utils12.access)(local.isDisabled)) {
      return;
    }
    (_a = local.onPressUp) == null ? void 0 : _a.call(local, {
      type: "pressup",
      pointerType: pointerType2,
      target: originalEvent.currentTarget,
      shiftKey: originalEvent.shiftKey,
      metaKey: originalEvent.metaKey,
      ctrlKey: originalEvent.ctrlKey,
      altKey: originalEvent.altKey
    });
  };
  const cancel = (e) => {
    if (!_isPressed()) {
      return;
    }
    if (isOverTarget()) {
      triggerPressEnd(createEvent(target(), e), pointerType(), false);
    }
    _setIsPressed(false);
    setIsOverTarget(false);
    setActivePointerId(null);
    setPointerType(null);
    removeAllGlobalListeners();
    if (!(0, import_utils12.access)(local.allowTextSelectionOnPress)) {
      restoreTextSelection(target());
    }
  };
  const globalOnKeyUp = (e) => {
    var _a, _b, _c, _d;
    if (_isPressed() && isValidKeyboardEvent(e)) {
      if (shouldPreventDefaultKeyboard(e.target)) {
        e.preventDefault();
      }
      e.stopPropagation();
      _setIsPressed(false);
      const eventTarget = e.target;
      triggerPressEnd(createEvent(target(), e), "keyboard", (_a = target()) == null ? void 0 : _a.contains(eventTarget));
      removeAllGlobalListeners();
      if (((_b = target()) == null ? void 0 : _b.contains(eventTarget)) && isHTMLAnchorLink(target()) || ((_c = target()) == null ? void 0 : _c.getAttribute("role")) === "link") {
        (_d = target()) == null ? void 0 : _d.click();
      }
    }
  };
  const globalOnPointerMove = (e) => {
    if (e.pointerId !== activePointerId()) {
      return;
    }
    if (isPointOverTarget(e, target())) {
      if (!isOverTarget()) {
        setIsOverTarget(true);
        triggerPressStart(createEvent(target(), e), pointerType());
      }
    } else if (isOverTarget()) {
      setIsOverTarget(false);
      triggerPressEnd(createEvent(target(), e), pointerType(), false);
      if ((0, import_utils12.access)(local.shouldCancelOnPointerExit)) {
        cancel(e);
      }
    }
  };
  const globalOnPointerUp = (e) => {
    if (e.pointerId === activePointerId() && _isPressed() && e.button === 0) {
      if (isPointOverTarget(e, target())) {
        triggerPressEnd(createEvent(target(), e), pointerType());
      } else if (isOverTarget()) {
        triggerPressEnd(createEvent(target(), e), pointerType(), false);
      }
      _setIsPressed(false);
      setIsOverTarget(false);
      setActivePointerId(null);
      setPointerType(null);
      removeAllGlobalListeners();
      if (!(0, import_utils12.access)(local.allowTextSelectionOnPress)) {
        restoreTextSelection(target());
      }
    }
  };
  const globalOnPointerCancel = (e) => {
    cancel(e);
  };
  const globalOnMouseUp = (e) => {
    if (e.button !== 0) {
      return;
    }
    _setIsPressed(false);
    removeAllGlobalListeners();
    if (ignoreEmulatedMouseEvents()) {
      setIgnoreEmulatedMouseEvents(false);
      return;
    }
    if (isPointOverTarget(e, target())) {
      triggerPressEnd(createEvent(target(), e), pointerType());
    } else if (isOverTarget()) {
      triggerPressEnd(createEvent(target(), e), pointerType(), false);
    }
    setIsOverTarget(false);
  };
  const globalOnScroll = (e) => {
    if (_isPressed() && e.target.contains(target())) {
      cancel({
        currentTarget: target(),
        shiftKey: false,
        ctrlKey: false,
        metaKey: false,
        altKey: false
      });
    }
  };
  const onKeyDown = (e) => {
    if (isValidKeyboardEvent(e) && e.currentTarget.contains(e.target)) {
      if (shouldPreventDefaultKeyboard(e.target)) {
        e.preventDefault();
      }
      e.stopPropagation();
      if (!_isPressed() && !e.repeat) {
        setTarget(e.currentTarget);
        _setIsPressed(true);
        triggerPressStart(e, "keyboard");
        addGlobalListener(document, "keyup", globalOnKeyUp, false);
      }
    }
  };
  const onKeyUp = (e) => {
    if (isValidKeyboardEvent(e) && !e.repeat && e.currentTarget.contains(e.target)) {
      triggerPressUp(createEvent(target(), e), "keyboard");
    }
  };
  const onClick = (e) => {
    if (e && !e.currentTarget.contains(e.target)) {
      return;
    }
    if (e && e.button === 0) {
      e.stopPropagation();
      if ((0, import_utils12.access)(local.isDisabled)) {
        e.preventDefault();
      }
      if (!ignoreClickAfterPress() && !ignoreEmulatedMouseEvents() && (pointerType() === "virtual" || isVirtualClick(e))) {
        if (!(0, import_utils12.access)(local.isDisabled) && !(0, import_utils12.access)(local.preventFocusOnPress)) {
          (0, import_utils11.focusWithoutScrolling)(e.currentTarget);
        }
        triggerPressStart(e, "virtual");
        triggerPressUp(e, "virtual");
        triggerPressEnd(e, "virtual");
      }
      setIgnoreEmulatedMouseEvents(false);
      setIgnoreClickAfterPress(false);
    }
  };
  const pressProps = {
    onKeyDown,
    onKeyUp,
    onClick
  };
  if (typeof PointerEvent !== "undefined") {
    pressProps.onPointerDown = (e) => {
      if (e.button !== 0 || !e.currentTarget.contains(e.target)) {
        return;
      }
      if (isVirtualPointerEvent(e)) {
        setPointerType("virtual");
        return;
      }
      if (shouldPreventDefault(e.currentTarget)) {
        e.preventDefault();
      }
      const newPointerType = setPointerType(e.pointerType);
      e.stopPropagation();
      if (_isPressed()) {
        return;
      }
      _setIsPressed(true);
      setIsOverTarget(true);
      setActivePointerId(e.pointerId);
      const newTarget = setTarget(e.currentTarget);
      if (!(0, import_utils12.access)(local.isDisabled) && !(0, import_utils12.access)(local.preventFocusOnPress)) {
        (0, import_utils11.focusWithoutScrolling)(e.currentTarget);
      }
      if (!(0, import_utils12.access)(local.allowTextSelectionOnPress)) {
        disableTextSelection(newTarget);
      }
      triggerPressStart(e, newPointerType);
      addGlobalListener(document, "pointermove", globalOnPointerMove, false);
      addGlobalListener(document, "pointerup", globalOnPointerUp, false);
      addGlobalListener(document, "pointercancel", globalOnPointerCancel, false);
    };
    pressProps.onMouseDown = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      if (e.button === 0) {
        if (shouldPreventDefault(e.currentTarget)) {
          e.preventDefault();
        }
        e.stopPropagation();
      }
    };
    pressProps.onPointerUp = (e) => {
      if (!e.currentTarget.contains(e.target) || pointerType() === "virtual") {
        return;
      }
      if (e.button === 0 && isPointOverTarget(e, e.currentTarget)) {
        triggerPressUp(e, pointerType() || e.pointerType);
      }
    };
    pressProps.onDragStart = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      cancel(e);
    };
  } else {
    pressProps.onMouseDown = (e) => {
      if (e.button !== 0 || !e.currentTarget.contains(e.target)) {
        return;
      }
      if (shouldPreventDefault(e.currentTarget)) {
        e.preventDefault();
      }
      e.stopPropagation();
      if (ignoreEmulatedMouseEvents()) {
        return;
      }
      _setIsPressed(true);
      setIsOverTarget(true);
      setTarget(e.currentTarget);
      const newPointerType = setPointerType(isVirtualClick(e) ? "virtual" : "mouse");
      if (!(0, import_utils12.access)(local.isDisabled) && !(0, import_utils12.access)(local.preventFocusOnPress)) {
        (0, import_utils11.focusWithoutScrolling)(e.currentTarget);
      }
      triggerPressStart(e, newPointerType);
      addGlobalListener(document, "mouseup", globalOnMouseUp, false);
    };
    pressProps.onMouseEnter = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      e.stopPropagation();
      if (_isPressed() && !ignoreEmulatedMouseEvents()) {
        setIsOverTarget(true);
        triggerPressStart(e, pointerType());
      }
    };
    pressProps.onMouseLeave = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      e.stopPropagation();
      if (_isPressed() && !ignoreEmulatedMouseEvents()) {
        setIsOverTarget(false);
        triggerPressEnd(e, pointerType(), false);
        if ((0, import_utils12.access)(local.shouldCancelOnPointerExit)) {
          cancel(e);
        }
      }
    };
    pressProps.onMouseUp = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      if (!ignoreEmulatedMouseEvents() && e.button === 0) {
        triggerPressUp(e, pointerType());
      }
    };
    pressProps.onTouchStart = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      e.stopPropagation();
      const touch = getTouchFromEvent(e);
      if (!touch) {
        return;
      }
      setActivePointerId(touch.identifier);
      setIgnoreEmulatedMouseEvents(true);
      setIsOverTarget(true);
      _setIsPressed(true);
      const newTarget = setTarget(e.currentTarget);
      const newPointerType = setPointerType("touch");
      if (!(0, import_utils12.access)(local.isDisabled) && !(0, import_utils12.access)(local.preventFocusOnPress)) {
        (0, import_utils11.focusWithoutScrolling)(e.currentTarget);
      }
      if (!(0, import_utils12.access)(local.allowTextSelectionOnPress)) {
        disableTextSelection(newTarget);
      }
      triggerPressStart(e, newPointerType);
      addGlobalListener(window, "scroll", globalOnScroll, true);
    };
    pressProps.onTouchMove = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      e.stopPropagation();
      if (!_isPressed()) {
        return;
      }
      const touch = getTouchById(e, activePointerId());
      if (touch && isPointOverTarget(touch, e.currentTarget)) {
        if (!isOverTarget()) {
          setIsOverTarget(true);
          triggerPressStart(e, pointerType());
        }
      } else if (isOverTarget()) {
        setIsOverTarget(false);
        triggerPressEnd(e, pointerType(), false);
        if ((0, import_utils12.access)(local.shouldCancelOnPointerExit)) {
          cancel(e);
        }
      }
    };
    pressProps.onTouchEnd = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      e.stopPropagation();
      if (!_isPressed()) {
        return;
      }
      const touch = getTouchById(e, activePointerId());
      if (touch && isPointOverTarget(touch, e.currentTarget)) {
        triggerPressUp(e, pointerType());
        triggerPressEnd(e, pointerType());
      } else if (isOverTarget()) {
        triggerPressEnd(e, pointerType(), false);
      }
      _setIsPressed(false);
      setActivePointerId(null);
      setIsOverTarget(false);
      setIgnoreEmulatedMouseEvents(true);
      if (!(0, import_utils12.access)(local.allowTextSelectionOnPress)) {
        restoreTextSelection(target());
      }
      removeAllGlobalListeners();
    };
    pressProps.onTouchCancel = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      e.stopPropagation();
      if (_isPressed()) {
        cancel(e);
      }
    };
    pressProps.onDragStart = (e) => {
      if (!e.currentTarget.contains(e.target)) {
        return;
      }
      cancel(e);
    };
  }
  (0, import_solid_js6.createEffect)((0, import_solid_js6.on)(() => (0, import_utils12.access)(local.allowTextSelectionOnPress), (allowTextSelectionOnPress) => {
    (0, import_solid_js6.onCleanup)(() => {
      if (!allowTextSelectionOnPress) {
        restoreTextSelection(target() ?? void 0);
      }
    });
  }));
  return {
    isPressed,
    pressProps: (0, import_props.combineProps)(domProps, pressProps)
  };
}
function isHTMLAnchorLink(target) {
  return target.tagName === "A" && target.hasAttribute("href");
}
function isValidKeyboardEvent(event) {
  const { key, code, target } = event;
  const element = target;
  const { tagName, isContentEditable } = element;
  const role = element.getAttribute("role");
  return (key === "Enter" || key === " " || key === "Spacebar" || code === "Space") && tagName !== "INPUT" && tagName !== "TEXTAREA" && isContentEditable !== true && (!isHTMLAnchorLink(element) || role === "button" && key !== "Enter") && !(role === "link" && key !== "Enter");
}
function getTouchFromEvent(event) {
  const { targetTouches } = event;
  if (targetTouches.length > 0) {
    return targetTouches[0];
  }
  return null;
}
function getTouchById(event, pointerId) {
  const changedTouches = event.changedTouches;
  for (let i = 0; i < changedTouches.length; i++) {
    const touch = changedTouches[i];
    if (touch.identifier === pointerId) {
      return touch;
    }
  }
  return null;
}
function createEvent(target, e) {
  return {
    currentTarget: target,
    shiftKey: e.shiftKey,
    ctrlKey: e.ctrlKey,
    metaKey: e.metaKey,
    altKey: e.altKey
  };
}
function getPointClientRect(point) {
  const offsetX = point.width && point.width / 2 || point.radiusX || 0;
  const offsetY = point.height && point.height / 2 || point.radiusY || 0;
  return {
    top: point.clientY - offsetY,
    right: point.clientX + offsetX,
    bottom: point.clientY + offsetY,
    left: point.clientX - offsetX
  };
}
function areRectanglesOverlapping(a, b) {
  if (a.left > b.right || b.left > a.right) {
    return false;
  }
  if (a.top > b.bottom || b.top > a.bottom) {
    return false;
  }
  return true;
}
function isPointOverTarget(point, target) {
  const rect = target.getBoundingClientRect();
  const pointRect = getPointClientRect(point);
  return areRectanglesOverlapping(rect, pointRect);
}
function shouldPreventDefault(target) {
  return !target.draggable;
}
function shouldPreventDefaultKeyboard(target) {
  return !((target.tagName === "INPUT" || target.tagName === "BUTTON") && target.type === "submit");
}
function isVirtualPointerEvent(event) {
  return event.width === 0 && event.height === 0 || event.width === 1 && event.height === 1 && event.pressure === 0 && event.detail === 0;
}

// src/createLongPress.ts
var DEFAULT_THRESHOLD = 500;
function createLongPress(props) {
  const defaultProps = {
    threshold: DEFAULT_THRESHOLD
  };
  props = (0, import_solid_js7.mergeProps)(defaultProps, props);
  let timeoutId;
  const { addGlobalListener, removeGlobalListener } = (0, import_utils14.createGlobalListeners)();
  const isDisabled = () => (0, import_utils15.access)(props.isDisabled) ?? false;
  const onPressStart = (e) => {
    var _a;
    if (e.pointerType === "mouse" || e.pointerType === "touch") {
      (_a = props.onLongPressStart) == null ? void 0 : _a.call(props, __spreadProps(__spreadValues({}, e), {
        type: "longpressstart"
      }));
      timeoutId = window.setTimeout(() => {
        var _a2;
        e.target.dispatchEvent(new PointerEvent("pointercancel", { bubbles: true }));
        (_a2 = props.onLongPress) == null ? void 0 : _a2.call(props, __spreadProps(__spreadValues({}, e), {
          type: "longpress"
        }));
        timeoutId = void 0;
      }, (0, import_utils15.access)(props.threshold) ?? DEFAULT_THRESHOLD);
      if (e.pointerType === "touch") {
        const onContextMenu = (e2) => {
          e2.preventDefault();
        };
        addGlobalListener(e.target, "contextmenu", onContextMenu, { once: true });
        addGlobalListener(window, "pointerup", () => {
          setTimeout(() => {
            removeGlobalListener(e.target, "contextmenu", onContextMenu);
          }, 30);
        }, { once: true });
      }
    }
  };
  const onPressEnd = (e) => {
    var _a;
    if (timeoutId) {
      window.clearTimeout(timeoutId);
    }
    if (e.pointerType === "mouse" || e.pointerType === "touch") {
      (_a = props.onLongPressEnd) == null ? void 0 : _a.call(props, __spreadProps(__spreadValues({}, e), {
        type: "longpressend"
      }));
    }
  };
  const { pressProps } = createPress({ isDisabled, onPressStart, onPressEnd });
  const description = () => {
    return props.onLongPress && !isDisabled() ? (0, import_utils15.access)(props.accessibilityDescription) : void 0;
  };
  const descriptionProps = (0, import_utils14.createDescription)(description);
  const longPressProps = (0, import_props2.combineProps)(pressProps, descriptionProps);
  return { longPressProps };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  PressResponderContext,
  createFocus,
  createFocusVisible,
  createFocusVisibleListener,
  createFocusWithin,
  createHover,
  createInteractOutside,
  createInteractionModality,
  createKeyboard,
  createLongPress,
  createPress,
  disableTextSelection,
  getInteractionModality,
  isKeyboardFocusVisible,
  restoreTextSelection,
  setInteractionModality,
  setupGlobalFocusEvents
});
