import linguiPlugin from './index.mjs';
import './shared/babel-plugin-lingui-macro.BYtM9YP7.mjs';
import '@babel/types';
import '@lingui/message-utils/generateMessageId';
import '@lingui/conf';

let deprecationWarningShown = false;
function macro({ state, babel, config }) {
  if (!deprecationWarningShown) {
    deprecationWarningShown = true;
    console.warn(
      `[@lingui/macro] Using Lingui macros via "babel-plugin-macros" is deprecated since v6 and will be removed in the next major version. Please migrate to the dedicated babel plugin "@lingui/babel-plugin-lingui-macro" or "@lingui/swc-plugin". See https://lingui.dev/installation?transpiler=babel#choosing-a-transpiler for installation instructions.`
    );
  }
  if (!state.get("linguiProcessed")) {
    state.opts = config;
    const plugin = linguiPlugin(babel);
    const { enter, exit } = plugin.visitor.Program;
    enter(state.file.path, state);
    state.file.path.traverse(plugin.visitor, state);
    exit(state.file.path, state);
    state.set("linguiProcessed", true);
  }
  return { keepImports: true };
}

export { macro };
